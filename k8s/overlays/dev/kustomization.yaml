apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - configmap.yaml
namePrefix: dev-
patches:
  - target:
      kind: Service
      name: node-app-service
    patch: |-
      - op: replace
        path: /spec/ports/0/nodePort
        value: 30081
